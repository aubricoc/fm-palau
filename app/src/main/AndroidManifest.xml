<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.aubricoc.palaudenoguera.festamajor2016">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_large"
        android:supportsRtl="true"
        android:theme="@style/fmpalau_theme">
        <activity
            android:name="cat.aubricoc.palaudenoguera.festamajor.activity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <data android:host="palau" android:scheme="fmpalau2016" />
            </intent-filter>
        </activity>
    </application>

</manifest>
